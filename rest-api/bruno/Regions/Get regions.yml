info:
  name: Get regions
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/api/regions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
