info:
  name: Get ADM entities in union
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/api/adm_entities/in_union"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
