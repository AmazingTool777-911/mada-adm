info:
  name: Get a region by id
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/api/regions/{{REGION_ID}}"
  params:
    - name: include_geojson
      value: ""
      type: query
      disabled: true
  auth: inherit

runtime:
  variables:
    - name: REGION_ID
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
