info:
  name: Get a fokontany's district
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/api/fokontanys/{{FOKONTANY_ID}}/district"
  params:
    - name: include_geojson
      value: ""
      type: query
      disabled: true
    - name: strategy
      value: ""
      type: query
      disabled: true
  auth: inherit

runtime:
  variables:
    - name: FOKONTANY_ID
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
