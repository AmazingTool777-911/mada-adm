info:
  name: Get Mada ADM config
  type: http
  seq: 8

http:
  method: GET
  url: "{{BASE_URL}}/api/config"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
