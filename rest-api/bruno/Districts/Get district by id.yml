info:
  name: Get district by id
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/api/districts/{{DISTRICT_ID}}"
  params:
    - name: include_geojson
      value: ""
      type: query
      disabled: true
  auth: inherit

runtime:
  variables:
    - name: DISTRICT_ID
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
