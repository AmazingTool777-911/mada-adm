info:
  name: Get the commune at a location
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/api/locations/{{LAT}}/{{LNG}}/commune"
  params:
    - name: include_geojson
      value: ""
      type: query
      disabled: true
  auth: inherit

runtime:
  variables:
    - name: LAT
      value: ""
    - name: LNG
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
