info:
  name: Get provinces
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/api/provinces"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
