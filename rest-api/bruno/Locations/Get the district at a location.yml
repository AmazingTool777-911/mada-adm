info:
  name: Get the district at a location
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/api/locations/{{LAT}}/{{LNG}}/district"
  params:
    - name: include_geojson
      value: ""
      type: query
      disabled: true
  auth: inherit

runtime:
  variables:
    - name: LAT
      value: ""
    - name: LNG
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
