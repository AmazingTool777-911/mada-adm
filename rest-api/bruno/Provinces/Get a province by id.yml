info:
  name: Get a province by id
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/api/provinces/{{PROVINCE_ID}}"
  params:
    - name: include_geojson
      value: ""
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
