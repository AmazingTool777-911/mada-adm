info:
  name: Get a fokontany by id
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/api/fokontanys/{{FOKONTANY_ID}}"
  params:
    - name: include_geojson
      value: ""
      type: query
      disabled: true
  auth: inherit

runtime:
  variables:
    - name: FOKONTANY_ID
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
